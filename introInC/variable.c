#include <stdio.h>

int main(){
    // int numberValue = 5;

    // printf("numberValue + 5 = %d\n", numberValue+ 5);

    int firstNumber = 100;
    int secondNumber = 200;
    printf("%d + %d = %d", firstNumber,secondNumber, firstNumber+ secondNumber);

    return 0;
}